#include "Point.h"
#include "Circle.h"

Circle::Circle(Point vcenter, int vradius) 
    : center(vcenter), radius(vradius) {
}

// Circle::draw(Point center, int radius)

// Circle::show(Point center, int radius)